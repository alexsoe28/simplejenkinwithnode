pipeline {
    agent any
    tools {nodejs "MyNode"}
    stages{
        stage("build") {
            steps {
              sh "node --version"
              sh "npm --version"
            }
        }
        stage ("Test") {
            steps {
                sh "node app.js"
            }
        }
        stage ("Deploy") {
            steps {
                echo "Deploy phase running"
            }
        }
    }
}
