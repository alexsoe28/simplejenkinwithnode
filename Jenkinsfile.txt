pipeline {
    agent any
    tools {nodejs "MyNode"}
    stages{
        stage("build") {
            steps {
              echo "node --version"
              echo "npm --version"
            }
        }
        stage ("Test") {
            steps {
                echo "node app.js"
            }
        }
        stage ("Deploy") {
            steps {
                echo "Deploy phase running"
            }
        }
    }
}
