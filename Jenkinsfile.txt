pipeline {
    agent any
    tools {nodejs "MyNode"}
    stages{
        stage("build") {
            steps {
              sh "node --version"
              sh "npm --version"
              sh "npm install"
            }
        }
        stage ("Test") {
            steps {
                sh "node server.js"
            }
        }
        stage ("Deploy") {
            steps {
                echo "Deploy phase running"
            }
        }
    }
}
